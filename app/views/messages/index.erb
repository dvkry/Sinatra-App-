<% @messages.each do |message| %>
  <div class="mesasges">
    <h3><%= message.title %> <small>(by: <%= message.author %>)</small>small></h3>
    <p>
      <%= message.content %>
    </p>
  </div>>
<% end %>